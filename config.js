// Paste your own Supabase project's URL and anon (public) key here.
// Get these from: Supabase dashboard → your project → Settings → API
//
// This game only uses Supabase Realtime (Broadcast + Presence) to relay
// messages between players — no database tables needed, so a free
// Supabase project works out of the box with zero extra setup beyond
// pasting these two values in.
//
// The anon key is meant to be public/client-safe — that's what it's for.

window.SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
